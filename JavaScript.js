/*
Javascript :
Il sert à rendre dynamique nos pages HTML/CSS
On fait tout même s'il a commencé en front
Logique utiliséepour les logiciels
Il a ce que les langages POO ont en général
+Variables, fonctions, tableaux, types, boucles, DOM, API, asynchrone backend avec Node.js
+Onglets, string, nombre, booléen, fonction en flèche
Emet en JS (raccourcis p.maclasse)

Notes CHROME ET SMART sur feuilles
*/

// Méthodes (fait référence à propriété dans un objet) callback utiles (fonctions de rappel)

let table = [];

let manif = [Thomas, Loïs, David];
let patri = [Jonas, Adrien];
let reli = [Helene, AnneLaure];
let ami = [Emilio, Yous];

manif.push(table);
patri.push(table);
reli.push(table);
ami.push(table);

console.log(table);
