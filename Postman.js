/*

Postman :

Apprendre à gerer le logiciel avec ses tests, gestion d'API et automatisations...

Postman utilise JSON et JavaScript

Swapi API, gratis et sur StarWars
Le navigateur en voie des requêtes Get, donc ajoute l'adresse dans la barre : http://swapi.dev/api/people

Nous voyons les personnages des fils officiels avec ses pairs clefs-valeurs
On va pouvoir tester les routes du CRUD (+ patch)

Body contient les infos, Header toute option qui est obligatoire
Status 200 ok, temps de parcours, taille, etc.

Le test d'API est d'enovyer et de communiquer avec son côté serveur en "send" des requêtes comme des posts.


Vous avez un onglet Test pour les tests unitaires en JavaScript.
Il est possible d'ajouter des modules {const = package(required)} et de les utiliser directement dans son indentation.
Permet de vérifier si l'API est toujours fonctionnelle, si on peut lui ajouter des requêtes.

Evitez de doubler les reponses.JSON pour ne pas perdre en performance.

L'adresse peut être développer selon des chemins "peoples", des :id, et {objet} ou {key}.

On peut créer des variables (globales) et les réutiliser dans son environnement et ses collections Postman (ajouts, déplacements, récupérations, duplications)

Il existe la variante pré-test en récupérant les keys des variables globales (on lance les tests et récupères ).

On découvre quelques fonctionnalités pratiques tels que les paramètrages d'URL et d'identifiants (avec token).

En effet, on peut développer à partir du fichier JS dans Node.js, ou bien directement avec Postman en gardant et envoyant le fichier de code construit.

NEWMAN, ajout pour Postman qui va nous aider dans nos requêtes.
YALM, package.JSON et GitLab : combinaison pour mettre à jour ses envoies.

*/
// Comment apprendre à utiliser Postman avec ses requêtes aux API, ses langages (JS, JSON, HTML, XML, etc.), ses objets et pairs clefs-valeurs, la route du CRUD, la gestion du Body et des Headers, sa possibilité de faire des tests et autres fonctionnalités.
